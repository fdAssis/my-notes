import React, { Component } from 'react';

class Form extends Component {
  render() {
    return (
      <form>
        <input />
        <textarea />
        <button>OK</button>
      </form>
    );
  }
}

export default Form;
